import 'package:flutter/material.dart';

class AppColors {
  // Primarios
  static const Color primaryBlue = Color(0xFF1A3B77);
  static const Color lightBlue = Color(0xFF284B94);
  static const Color accentYellow = Color(0xFFF7C840);

  // Fondo y superficies
  static const Color lightBackground = Color(0xFFF2F4F6);
  static const Color white = Colors.white;
  static const darkBlue = Color(0xFF284B94);

  // Texto
  static const Color textDark = Color(0xFF3A3A3A);
  static const Color textLight = Colors.white70;

  // Otros
  static const MaterialColor gray = Colors.grey;
  static const Color transparent = Colors.transparent;
}
